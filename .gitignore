/node_modules
/public/hot
/public/storage
/storage/*.key
/storage/grand-backups
/vendor
/.idea
/.vagrant
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
.env
.phpunit.result.cache
.phpunit.cache
.sql
.codebuddy

# Rust compilation dirs
rust/*/target
# Compiled rust apps (rust modules are recompiled during container start)
storage/rust-libs/*.so

# Local output from in-game JavaScript tooling.
tmp/

# Separately-maintained modules live in their own repositories (their own clone,
# their own remote). The host ignores the path instead of pinning a submodule.
/Modules/AI/
